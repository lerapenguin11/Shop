<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Shop" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="colorPrimary">@color/pink</item>
        <item name="colorOnSurface">#FF8AC9</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Shop" parent="Base.Theme.Shop" />

    <style name="Widget.BottomNavigationViewActive"
        parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/sf_ui_display_regular</item>
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">@color/pink</item>
    </style>

    <style name="Widget.BottomNavigationViewNotActive"
        parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/sf_ui_display_regular</item>
        <item name="android:textSize">10sp</item>
        <item name="android:textColor">@color/dark_grey</item>
    </style>
</resources>